PORT=5050
CLIENT_ORIGIN=http://localhost:5173

# Optional (metadata persistence). If unset, the app still works (in-memory repo index).
MONGODB_URI=mongodb://127.0.0.1:27017/repopilot

# Where ingested repos live (relative to backend/)
REPOS_DIR=repos
TMP_DIR=tmp

# LLM / Embeddings (Phase 2+)
# Provider currently supported: openai
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-4.1-mini
OPENAI_EMBED_MODEL=text-embedding-3-small

# Indexing / Vector cache
VECTORS_DIR=.cache/vectors

